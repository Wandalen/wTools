[package]
name = "proc_macro_tools_trivial_sample"
version = "0.0.0"
edition = "2021"
publish = false

[features]
use_std = []
default = [ "use_std" ]

[dependencies]
proc_macro_tools = { version = "*", path = "../../../module/rust/proc_macro_tools" }
