[package]
name = "error_tools_trivial_sample"
version = "0.0.0"
edition = "2021"
publish = false

[features]
use_std = []
default = [ "use_std" ]

[dependencies]
error_tools = { version = "*", path = "../../../module/rust/error_tools" }
