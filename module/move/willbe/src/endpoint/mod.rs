crate::mod_interface!
{
  /// List packages.
  layer list;
  /// Publish packages.
  layer publish;
  /// Tables.
  layer table;
  /// Run all tests
  layer test;
  /// Workflow.
  layer workflow;
  /// Workspace new.
  layer workspace_new;
  /// Deploy new.
  layer deploy_new;
  /// Main Header.
  layer main_header;
  /// Module headers.
  layer module_headers;
}
