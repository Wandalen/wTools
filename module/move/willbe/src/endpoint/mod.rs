crate::mod_interface!
{
  /// List packages.
  layer list;
  /// Publish packages.
  layer publish;
  /// Tables.
  layer table;
  /// Run all tests
  layer run_tests;
  /// Workflow.
  layer workflow;
  /// Module headers.
  layer module_headers;
}
