crate::mod_interface!
{
  /// Container for contexts values
  layer context;
  /// `ExecutableCommand` representation
  layer command;
  /// Command callback representation
  layer routine;
}

// qqq : for Bohdan : to many levels, what about removing this folder and attaching its files to parent folder?
