# tests/docs/feature

Feature test surface specs for `optimize_split!` and `optimize_match!` macros.

## Scope

- **In Scope**: Behavioral test cases for compile-time split and match features.
- **Out of Scope**: API contract cases (see `../api/`); invariant enforcement (see `../invariant/`).
- **Status**: Active.

## Responsibility Table

| File | Responsibility | Status |
|------|----------------|--------|
| `001_compile_time_split.md` | FT cases for optimize_split! behavior | ✅ |
| `002_compile_time_match.md` | FT cases for optimize_match! behavior | ✅ |
