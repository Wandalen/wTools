# tests/docs/api

API test surface specs for `optimize_split!` and `optimize_match!` macros.

## Scope

- **In Scope**: API contract test cases — invocation forms, parameter defaults, return types, compile-time error conditions.
- **Out of Scope**: Behavioral feature cases (see `../feature/`); invariant enforcement (see `../invariant/`).
- **Status**: Active.

## Responsibility Table

| File | Responsibility | Status |
|------|----------------|--------|
| `001_optimize_split_api.md` | AP cases for optimize_split! API contract | ✅ |
| `002_optimize_match_api.md` | AP cases for optimize_match! API contract | ✅ |
