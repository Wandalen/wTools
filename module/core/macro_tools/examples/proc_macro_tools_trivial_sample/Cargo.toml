[package]
name = "proc_macro_tools_trivial_sample"
version = "0.0.0"
edition = "2021"
publish = false

[features]
# use_std = []
no_std = []
default = [ ]

[dependencies]
macro_tools = { workspace = true }
