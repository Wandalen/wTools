# 001 — Fix `use` keyword in `mod_interface!` not propagating child layer items

## Status: 📥 Ready

- **ID:** 001
- **Priority:** 8
- **Executor:** any
- **Advisability:** 1944
- **Value:** 9 / Easiness:** 5 / Safety:** 3

## Purpose

`mod_interface!{ use super::layer_a; }` is supposed to pull all re-exported items from
`layer_a` into the parent module's visibility layers (`orphan`, `exposed`, `prelude`, `own`).
Instead the generated re-exports are missing — 121 E0425 errors across tests and examples.

## Context

The `use` keyword inside `mod_interface!` is a declarative directive meaning:
"import this child module's mod_interface-exported items into my own layers."

Example: parent module declares `use super::layer_a` — `layer_a` itself has
`mod_interface!{ orphan use PrivateStruct1; orphan use SubStruct2; }`.
Expected: `parent::orphan::PrivateStruct1` resolves.
Actual: E0425 — `PrivateStruct1` not found in `orphan`.

The compiler suggests `use crate::child::prelude_thing` (the item exists in the child's
layers) but denies access via the parent's layers — the cross-module re-export chain
generated by the macro is broken.

## Affected Files

**Example (11 errors):**

- `examples/mod_interface_trivial/src/main.rs` — asserts `prelude_thing`, `exposed_thing`,
  `orphan_thing`, `own_thing` accessible via parent's `prelude`/`own`/`orphan`/`exposed`

**Tests (110 errors) — all in `tests/inc/`:**

- `derive/layer_have_layer_separate_use/mod.rs` + `only_test/layer_simple_only_test.rs`
- `derive/layer_have_layer_separate_use_two/mod.rs` + same only_test
- `derive/layer_use_cfg/mod.rs` + same only_test
- `derive/use_as/derive.rs` + `only_test/layer_single_only_test.rs`
- `derive/use_basic/mod.rs` + `only_test/layer_simple_only_test.rs`
- `derive/use_layer/mod.rs` + `only_test/use_non_layer_only_test.rs`
- `manual/layer/layer_a.rs`, `manual/layer/layer_b.rs`

Error counts: 121 total (11 example + 110 tests).

## Root Cause Hypothesis

The `use` keyword handling in the `mod_interface!` proc-macro (in `mod_interface_meta`)
generates `use child::own::*` / `use child::orphan::*` etc. style re-exports for the
parent module's layer modules. One or more of these generated `use` statements is either:

(a) not generated at all for the `use super::layer_a` variant, or
(b) generated with a wrong visibility (`pub(crate)` vs `pub`), or
(c) generated with a wrong path when the child module is a sibling (non-inline) module.

Investigation should start in `mod_interface_meta/src/` — specifically the `use` keyword
arm of the main `mod_interface!` expansion logic.

## MOST Goals

1. `cargo build --manifest-path .../Cargo.toml --example mod_interface_trivial --all-features`
   exits 0 — no E0425 errors in the example
2. `cargo nextest run --manifest-path .../Cargo.toml -p mod_interface --all-features 2>&1 | grep "E0425" | wc -l`
   returns 0
3. All previously-passing mod_interface tests continue to pass — no regressions

## Validation Criteria (Done When)

- [ ] `cargo build -p mod_interface --example mod_interface_trivial --all-features` → exit 0
- [ ] `cargo nextest run -p mod_interface --all-features` → 0 test failures, 0 E0425 errors
- [ ] `cargo clippy -p mod_interface --all-targets --all-features -- -D warnings` → exit 0
- [ ] `cargo clippy -p mod_interface_meta --all-targets --all-features -- -D warnings` → exit 0

## Work Procedure

1. Read `mod_interface_meta/src/` to find where `use` keyword inside `mod_interface!` is handled
2. Read `tests/inc/derive/use_basic/` to understand the simplest failing case
3. Compare generated code (via `cargo expand`) for a `use super::child` call vs a working directive
4. Identify missing or malformed re-export in the generated code
5. Fix the code generation in `mod_interface_meta`
6. Re-run validation criteria — iterate until all 4 pass

## Notes

- The `mod_interface` library itself builds fine (`cargo build -p mod_interface --all-features` → exit 0);
  the failures are only in tests and examples that exercise the `use` keyword directive.
- The `-p meta_tools` L3 validation is also blocked by these same errors (pre-existing).
- Safety rating 3 because `mod_interface` is used by ~30 crates in the workspace — incorrect
  macro expansion changes could cascade widely.
