[package]
name = "mod_interface_trivial"
version = "0.0.0"
edition = "2021"
publish = false

# Standalone package - not a workspace member
[workspace]

[dependencies]
mod_interface = { path = "../..", features = [ "enabled" ] }
