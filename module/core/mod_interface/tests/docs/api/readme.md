# tests/docs/api

Test specs for `docs/api/` instances of the `mod_interface` crate.

## File Responsibility Table

| Entry | Responsibility |
|-------|----------------|
| `001_mod_interface_macro_test.md` | Coverage matrix for the mod_interface! macro DSL API |
