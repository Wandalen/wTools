# Unverified Tasks

### Scope

**Responsibility**: Hold new task files until MAAV Verification Gate passes.
**In Scope**: Task files in state ❓ Unverified awaiting 4-subagent verification.
**Out of Scope**: Active tasks (see task root), completed tasks (see `completed/`).

### Responsibility Table

| File | Responsibility |
|------|----------------|
| `011_format_aligned_display_width_mismatch.md` | Fix display-width mismatch in format_aligned two-pass algorithm |
