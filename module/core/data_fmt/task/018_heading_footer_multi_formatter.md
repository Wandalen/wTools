# 018 — Header and footer titled-rule support across formatters

## MOST Goal

Generalize the existing `Heading` titled-rule pattern (`─── Title · field ─────...`) along two independent axes it was previously restricted on: **position** (header-only → header + footer) and **formatter coverage** (Table-only → Table/Tree/Expanded/Text/Yaml/Toml/Sql), so any comment-capable or plain-text formatter can bracket its output with a consistent opening and/or closing titled rule.

- **Motivated:** `Heading` (`src/config/table_heading.rs`, formerly `TableCaption`, renamed under task 009) already renders a titled divider rule, but only ever *above* a table (`TableConfig::with_heading()`) and only for `TableFormatter`. Both restrictions are historical accidents of where the feature was first built, not deliberate design boundaries — the rendering logic (`content_str()`, lead/trail width arithmetic, CJK-safe `unicode_visual_len` measurement) has no inherent dependency on "above" placement or on the Table formatter specifically. A footer position (closing rule below the output) and coverage across every other formatter that already emits fixed-width or comment-syntax text closes an obvious, low-risk gap using code that already exists and is already battle-tested (BUG-015/016/017 fixes apply automatically to every new call site because they all route through one shared function).
- **Observable:** `TableConfig::with_footer(Heading)` renders a titled rule after the bottom border, symmetric to `with_heading()` before the top border; both may be set independently or together with no interference. The same header/footer pair becomes available on `TreeFormatter`, `ExpandedFormatter`, and `TextFormatter` configs, and on `Yaml`/`Toml`/`Sql` formatters wrapped in each format's native comment syntax (`#` / `--`). `Json`, `Logfmt`, and `Html` are explicitly excluded (no comment syntax for the first two; `Html` deferred pending its own design). Every change is additive — `Option<Heading>` fields defaulting to `None` — so no existing caller's output changes unless it opts in.
- **Scoped:** one shared rendering primitive (`render_rule_if_present`, refactored from the Table-only `render_heading_if_present`) parameterized by the `Heading` value and target width rather than reading a fixed config field, reused verbatim at every new call site; per-formatter config additions (`footer`/`with_footer()`/`footer_ref()` on `TableConfig`, mirrored on Tree/Expanded/Text configs); comment-wrapped variants for Yaml/Toml/Sql; matching test coverage per formatter mirroring the existing `table_heading_test.rs` pattern; `docs/feature/007_table_heading.md`, `docs/algorithm/007_heading_rendering.md`, `docs/invariant/005_heading.md`, `docs/api/003_config_types.md` and their `tests/docs/` counterparts extended (not replaced) to describe both positions and all covered formatters. No change to `Heading`'s own data shape, to any existing formatter's default (no-heading/no-footer) output, or to Json/Logfmt/Html.
- **Testable:** `cargo nextest run -p data_fmt --all-features --test table_heading_test --test table_footer_test` (Phase 1, done) plus one mirrored test file per subsequent phase; `cargo clippy -p data_fmt --all-targets --all-features -- -D warnings` clean after every phase; full-suite regression (`cargo nextest run -p data_fmt --all-features`) shows zero change to any pre-existing test.

## In Scope

- **Phase 0 (done):** Generalize `Heading`'s doc comment (`src/config/table_heading.rs`) from header-only framing to "rendered as a header (above) or footer (below)".
- **Phase 1 (done):** `TableFormatter` footer.
  - `src/formatters/table/row_rendering.rs` — renamed `render_heading_if_present` → `render_rule_if_present`, parameterized by `rule: Option<&Heading>` instead of reading `self.config.heading_ref()` internally; made an associated function (no `&self`) since the body no longer touches any other formatter state (clippy `unused_self`).
  - `src/config/table_config.rs` — added `footer: Option<Heading>` field, `with_footer()` builder, `footer_ref()` accessor, mirroring `heading`/`with_heading()`/`heading_ref()` exactly.
  - `src/formatters/table/mod.rs` — added a footer call site after the bottom border; updated the existing heading call site to the new shared function signature.
  - `tests/table_footer_test.rs` (new, 12 tests, FT-10..FT-18 plus 3 invariant/regression reproducers) mirroring `tests/table_heading_test.rs`.
  - Docs extended: `docs/feature/007_table_heading.md`, `docs/algorithm/007_heading_rendering.md`, `docs/invariant/005_heading.md`, `docs/api/003_config_types.md`, and their three `tests/docs/` counterparts.
- **Phase 2 (pending):** `TreeFormatter` heading + footer — footer is a direct mirror of Phase 1's pattern; header requires confirming whether `TreeFormatter::format` streams output incrementally or builds a buffer before returning (unread as of Phase 1 close — first action of Phase 2), since a header needs the final rendered width known before the first line is emitted.
- **Phase 3 (pending):** `ExpandedFormatter` heading + footer — must stay a distinct concept from the formatter's existing per-record `record_separator`; requires reading `src/formatters/expanded/mod.rs` in full before implementation.
- **Phase 4 (pending):** `TextFormatter` heading + footer — no separate `TextConfig` type exists today; fields are added directly to `TextFormatter` rather than inventing a config type that nothing else needs (YAGNI).
- **Phase 5 (pending):** `Yaml`/`Toml`/`Sql` formatters — heading/footer wrapped in each format's native comment prefix (`#` for Yaml/Toml, `--` for Sql), reusing the same width algorithm.

## Out of Scope

- `Json` (no comment syntax to host a non-data line), `Logfmt` (no comment convention), `Html` (deferred — needs its own header/footer design, e.g. `<!-- -->` vs. a visible banner element; not a mechanical port of the text-rule pattern).
- Any change to `Heading`'s own fields, `content_str()` format, or the `·`/`─` constants.
- Any change to existing no-heading/no-footer output on any formatter (Invariant 1 — passthrough — holds throughout).
- A crate version bump: consistent with task 009 (39 new `with_` builders, no bump) rather than 016/017 (bumped because a specific external consumer needed a pinned version) — no external consumer has requested a pinned version for this feature; deferred unless one materializes.

## Work Procedure

1. ~~Phase 0: generalize `Heading` doc comment.~~ Done.
2. ~~Phase 1: `TableFormatter` footer — config field/builder/accessor, position-agnostic shared render function, call site, tests, docs.~~ Done — 35/35 tests pass (`table_heading_test` + `table_footer_test`), clippy clean.
3. ~~Phase 2: read `src/formatters/tree/mod.rs` in full; implement footer, then header; mirror test file; extend the same doc set.~~ Done — 6/6 new tests pass (`tree_heading_test.rs`, FT-19..FT-24), full suite 967/967, clippy clean. `TreeFormatter::format()` was confirmed to already fully buffer into a `String` before returning — no streaming existed anywhere in Tree's formatters — so no buffer-then-prepend restructuring was needed, simplifying the phase versus the original open question.
4. ~~Phase 3: read `src/formatters/expanded.rs` in full (task text said `expanded/mod.rs` — corrected; Expanded is a single file, not a directory); implement heading + footer distinct from `record_separator`; mirror test file; extend docs.~~ Done — 6/6 new tests pass (`expanded_heading_test.rs`, FT-25..FT-30), full suite 973/973, clippy clean. `ExpandedConfig` uses all-`pub` fields (no accessor methods, unlike `TableConfig`/`TreeConfig`) — `with_heading()`/`with_footer()` added following that file's own established convention rather than force-fitting Tree's private-field-plus-accessor pattern.
5. ~~Phase 4: read `src/formatters/text.rs` in full; add heading/footer fields directly to `TextFormatter`; mirror test file; extend docs.~~ Done — 6/6 new tests pass (`text_heading_test.rs`, FT-31..FT-36), full suite 979/979, clippy clean. `TextFormatter` has no separate config type — `heading`/`footer` are plain `pub` fields directly on the formatter (matching Expanded's all-`pub` convention), wired into the single funnel point where `Format::format()` calls `wrap_with_heading_footer()` on its `match`-produced body before returning.
6. ~~Phase 5: read the Yaml/Toml/Sql formatter sources in full; implement comment-wrapped heading/footer reusing the shared width algorithm; mirror test files; extend docs.~~ Done — 16/16 new tests pass (`yaml_heading_test.rs` FT-37..41, `toml_heading_test.rs` FT-42..46, `sql_heading_test.rs` FT-47..52), full suite 998/998, clippy clean. `YamlFormatter`/`TomlFormatter` converted from zero-field unit structs to 2-field structs (`heading`/`footer`, all-`pub`, matching Expanded/Text's convention); `SqlFormatter` gained the same 2 fields alongside its pre-existing 3. Reused `Heading::render_line` unmodified via a new `render_commented_rule_if_present` wrapper (`# `/`-- ` prefix) rather than changing `Heading` itself. Found and fixed a real defect: `SqlFormatter`'s populated-rows body ends in a bare `;` with no trailing `\n` (unlike every other formatter's body, which always ends in `\n`), so appending a footer directly concatenated onto the closing `;`'s line — fixed by inserting a separating `\n` in `wrap_with_heading_footer` when a footer is present, the body is non-empty, and it doesn't already end in `\n`.
7. ~~Final verification once all phases land: full-suite `cargo nextest run -p data_fmt --all-features`, full `cargo clippy -p data_fmt --all-targets --all-features -- -D warnings`, revisit the version-bump question against whatever consumer situation exists at that point.~~ Done — full suite 998/998 pass, clippy clean (see Phase 5 verification above). No external consumer requiring a version bump has materialized (same non-decision as Phases 1–4, per Out-of-Scope).

## Test Matrix

| Input Scenario | Config Under Test | Expected Behavior |
|----------------|-------------------|--------------------|
| `TableConfig::plain().with_footer(Heading::new("Bottom"))` | title-only footer | line starts `─── Bottom`, fills to `table_width` |
| footer with fields | `Heading::new("X").with_field("Y")` | content joined `X · Y` |
| footer content width == `table_width` | boundary | no trailing rule chars, no panic |
| footer content width > `table_width` | overflow | trailing rule clamped to 0; content never truncated (documented, not a bug) |
| no footer set | passthrough | output byte-identical to no-footer baseline (Invariant 1) |
| footer on `AsciiGrid`/`Unicode` bordered table | after bottom border | footer line follows the bottom border line exactly |
| heading + footer both set | coexistence | first line is heading, last line is footer, header row still present between them, each independently matches its solo-rendering equivalent |
| CJK title in footer | BUG-015 regression | display width (not char count) matches table body width |
| newline embedded in footer title | BUG-016 regression | sanitized to a single output line |
| footer on bordered table | BUG-017 regression | display width matches table body width including border padding |
| `TreeConfig::new().with_heading(Heading::new("Project"))` | title-only heading | first line starts `─── Project`, tree body unaffected |
| `TreeConfig::new().with_footer(Heading::new("2 items"))` | title-only footer | last line starts `─── 2 items`, tree body unaffected |
| heading on tree with ragged line lengths | target-width derivation | heading fills to the widest rendered body line's display width, not a fixed table-style width |
| no heading/footer set on `TreeConfig` | passthrough | output byte-identical to `TreeFormatter::new()` baseline (Invariant 1) |
| heading + footer both set on tree | coexistence | first line is heading, last line is footer, tree body between them unaffected |
| heading on leaf-only-root tree (data, no children) | branch coverage | heading still applies; output is exactly 2 lines (heading + leaf line) |
| `ExpandedConfig::new().with_heading(Heading::new("Users"))` | title-only heading | first line starts `─── Users`, expanded body unaffected |
| `ExpandedConfig::new().with_footer(Heading::new("2 records"))` | title-only footer | last line starts `─── 2 records`, expanded body unaffected |
| heading on expanded output with ragged line lengths | target-width derivation | heading fills to the widest rendered body line's display width, same technique as Tree |
| no heading/footer set on `ExpandedConfig` | passthrough | output byte-identical to `ExpandedFormatter::new()` baseline (Invariant 1) |
| heading + footer both set on expanded output | coexistence | first line is heading, last line is footer, expanded body between them unaffected |
| heading on empty-headers view (zero columns) | branch coverage | heading still applies; output is exactly 1 line (heading only, no body) |
| `ExpandedConfig::new().with_heading(...)` alongside default `record_separator` | distinctness | heading/footer wrap the whole output once each; `record_separator` still repeats once per record, independently |
| `TextFormatter::bullets().with_heading(Heading::new("Users"))` | title-only heading | first line starts `─── Users`, text body unaffected |
| `TextFormatter::bullets().with_footer(Heading::new("2 records"))` | title-only footer | last line starts `─── 2 records`, text body unaffected |
| heading on text output with ragged line lengths across `TextVariant` styles | target-width derivation | heading fills to the widest rendered body line's display width, same technique as Tree/Expanded |
| no heading/footer set on `TextFormatter` | passthrough | output byte-identical to `TextFormatter::new(TextVariant::Bullets)` baseline (Invariant 1) |
| heading + footer both set on text output | coexistence | first line is heading, last line is footer, text body between them unaffected |
| heading on `TextVariant::CliHelp` with zero rows | branch coverage | heading still applies; output is exactly 1 line (heading only), since `format_cli_help()`'s `data.rows.is_empty()` early return still flows through the single `Format::format()` funnel point |
| `YamlFormatter::new().with_heading(Heading::new("Users"))` | title-only heading | first line starts `# ─── Users`, YAML body unaffected |
| `YamlFormatter::new().with_footer(Heading::new("2 records"))` | title-only footer | last line starts `# ─── 2 records`, YAML body unaffected |
| heading on YAML output with ragged line lengths | target-width derivation | commented heading's total width (prefix + rule) fills to the widest rendered body line's display width, same technique as Tree/Expanded/Text |
| no heading/footer set on `YamlFormatter` | passthrough | output is pure unwrapped `serde_yaml_ng` output — no leading `#` line, still starts with `-` |
| heading + footer both set on YAML output | coexistence | first line is heading, last line is footer, YAML body between them unaffected |
| `TomlFormatter::new().with_heading(Heading::new("Users"))` | title-only heading | first line starts `# ─── Users`, TOML body unaffected |
| `TomlFormatter::new().with_footer(Heading::new("2 records"))` | title-only footer | last line starts `# ─── 2 records`, TOML body unaffected |
| heading on TOML output with ragged line lengths | target-width derivation | commented heading's total width fills to the widest rendered body line's display width, same technique as Yaml |
| no heading/footer set on `TomlFormatter` | passthrough | output is pure unwrapped `toml::to_string` output — no leading `#` line, still starts with `[[row]]` |
| heading + footer both set on TOML output | coexistence | first line is heading, last line is footer, TOML body between them unaffected |
| `SqlFormatter::new("users").with_heading(Heading::new("Users"))` | title-only heading | first line starts `-- ─── Users`, SQL body unaffected |
| `SqlFormatter::new("users").with_footer(Heading::new("2 records"))` | title-only footer | last line starts `-- ─── 2 records`, SQL body unaffected — requires a separating `\n` since the populated-rows body ends in a bare `;` (BUG found and fixed in Phase 5, see Work Procedure item 6) |
| heading on SQL output with ragged line lengths | target-width derivation | commented heading's total width fills to the widest rendered body line's display width, same technique as Yaml/Toml |
| no heading/footer set on `SqlFormatter` | passthrough | output is a pure unwrapped `INSERT INTO ...;` statement — no leading `--` line |
| heading + footer both set on SQL output | coexistence | first line is heading, last line is footer, SQL body between them unaffected |
| heading on `SqlFormatter` with zero rows (BUG-020 early return) | branch coverage | heading still applies; output is exactly 1 line (heading only), since the `data.rows.is_empty()` early return flows through the same `wrap_with_heading_footer` call as the populated-rows path |

## Execution State

- **State:** 🎯 (Verified — in progress, Phase 4/6 complete)
- **ID:** 018
- **Slug:** heading_footer_multi_formatter
- **Executor:** ai
- **Priority:** 4
- **Value:** 7
- **Easiness:** 7
- **Safety:** 9
- **Dir:** `module/core/data_fmt`
- **Closes:** null
- **Reopen Count:** 0

## History

- **[2026-08-22]** `CREATED` — Filed retroactively to document a 6-phase initiative approved and begun earlier in the same session (Phase 0 + Phase 1 already executed at filing time). Generalizes `Heading` from header-only/Table-only to header+footer across Table/Tree/Expanded/Text/Yaml/Toml/Sql, reusing the existing rendering primitive verbatim so historical bug fixes (BUG-015/016/017) apply to every new call site automatically.
- **[2026-08-22]** `VERIFIED` — Readiness Verification Gate PASS (Tier 2 Dual-Role Self-Check, self-administered by ai; see Verification Record). State ❓ → 🎯; registered in the data_fmt Tasks Index.
- **[2026-08-22]** `PHASE1_COMPLETE` — `TableFormatter` footer support shipped: `TableConfig::with_footer()`/`footer_ref()`, position-agnostic `render_rule_if_present` (associated fn, fixes a clippy `unused_self` introduced by the refactor), footer call site after the bottom border, 12 new tests (`tests/table_footer_test.rs`, FT-10..FT-18 + 3 regression reproducers), full doc set extended. `cargo nextest run -p data_fmt --all-features --test table_footer_test --test table_heading_test` → 35/35 pass; `cargo clippy -p data_fmt --all-targets --all-features -- -D warnings` → clean. An initial run's `heading_example_binaries_compile_and_produce_output_ft9` failure was diagnosed as a transient concurrent-`cargo`-build artifact (unrelated pre-existing test that shells out to `cargo run` mid-test) — confirmed not a regression via isolated re-run, then reconfirmed passing in the full concurrent run above.
- **[2026-08-22]** `PHASE2_COMPLETE` — `TreeFormatter` heading + footer support shipped: `TreeConfig::with_heading()`/`with_footer()`/`heading_ref()`/`footer_ref()` (mirroring `TableConfig` exactly), `wrap_with_heading_footer()` wired into `TreeFormatter::format()` only — its 3 internal branches (leaf-only-root, empty-root, standard) all converge on one tail call; `format_aligned()`/`format_with_aggregation()` deliberately left untouched, matching the task's own approved Phase 2 scope. `render_rule_if_present`/`Heading::render_line()` relocated from Table-specific `formatters/table/row_rendering.rs` to formatter-agnostic `config/table_heading.rs` (`pub(crate)`, not `pub` — avoids unilaterally resolving task 015's separate public-API question) so every current and future formatter shares one implementation; Table's two call sites updated to the new location with zero behavior change. Tree's `target_width` is computed as the max display width across the already-rendered body's lines (Tree has no fixed column width the way Table does) — confirmed `TreeFormatter::format()` already fully buffers into a `String` before returning, so no buffer-then-prepend restructuring was needed, correcting the Phase 2 Work Procedure's original streaming-vs-buffer open question. 6 new tests (`tests/tree_heading_test.rs`, FT-19..FT-24). Doc sweep completed across `docs/algorithm/007_heading_rendering.md`, `docs/invariant/005_heading.md`, `docs/feature/007_table_heading.md` (H1 retitled "Heading and Footer Titled Rules"; file itself not renamed), `docs/api/003_config_types.md`, all 3 `tests/docs/` counterparts, and `tests/readme.md`; incidentally fixed a Phase-1-introduced doc/code drift (FT-18 spec still described the old "Bottom" footer title after the source fixture was renamed to "End"). Fresh live verification after the full doc sweep: `cargo nextest run -p data_fmt --all-features` → 967/967 pass; `cargo clippy -p data_fmt --all-targets --all-features -- -D warnings` → clean.
- **[2026-08-22]** `PHASE3_COMPLETE` — `ExpandedFormatter` heading + footer support shipped: `ExpandedConfig::with_heading()`/`with_footer()` added as plain `pub` fields (not private+accessor like `TableConfig`/`TreeConfig`) — matching `ExpandedConfig`'s own pre-existing all-`pub`-fields convention (confirmed by reading all 7 existing fields before editing) rather than force-fitting Tree's private-field-plus-accessor pattern; call sites in `expanded.rs` use `self.config.heading.as_ref()` / `self.config.footer.as_ref()` directly. `wrap_with_heading_footer()` wired into both of `ExpandedFormatter::format_view()`'s return points — the `headers.is_empty()` early return and the final tail return after the row-building loop — the formatter's single funnel point (simpler than Tree's 3-branch convergence in Phase 2, since Expanded already had exactly one internal core method feeding all three public entry points). `target_width` computed identically to Tree: max display width across the already-rendered body's lines, since Expanded output is equally ragged (no fixed column width). Explicitly kept distinct from the pre-existing per-record `record_separator` — heading/footer bracket the whole output once each, `record_separator` repeats per record inside the body; both remain independently settable. 6 new tests (`tests/expanded_heading_test.rs`, FT-25..FT-30, including an empty-headers branch-coverage case mirroring Tree's leaf-only-root FT-24). Doc sweep completed across `docs/algorithm/007_heading_rendering.md`, `docs/invariant/005_heading.md`, `docs/feature/007_table_heading.md` (added "Expanded usage" construction example), `docs/api/003_config_types.md` (`#### ExpandedConfig` extended 7→9 fields/builders; incidentally fixed a pre-existing doc/code drift in `#### WrapConfig` that claimed `ExpandedConfig`'s fields were private — they are actually all `pub`, confirmed by reading the source), all 3 `tests/docs/` counterparts, and `tests/readme.md`. Live verification during implementation (same uncompacted session, no rerun needed for the doc-only sweep that followed): `cargo check -p data_fmt --all-features` clean; `cargo nextest run -p data_fmt --all-features --test expanded_heading_test` → 6/6 pass; `cargo nextest run -p data_fmt --all-features` (full suite) → 973/973 pass; `cargo clippy -p data_fmt --all-targets --all-features -- -D warnings` → clean.
- **[2026-08-22]** `PHASE4_COMPLETE` — `TextFormatter` heading + footer support shipped: `heading: Option<Heading>` / `footer: Option<Heading>` added as plain `pub` fields directly on `TextFormatter` — no separate `TextConfig` type was created, per the task's explicit YAGNI decision (In Scope, Phase 4) — matching `ExpandedConfig`'s all-`pub`, no-accessor convention rather than inventing a config type nothing else needs; `with_heading()`/`with_footer()` builder setters added alongside the pre-existing `with_indent()`/`with_separator()`. Both fields are wired into exactly one funnel point: `impl Format for TextFormatter { fn format(...) }`'s single `match` over the 6 `TextVariant` arms produces a `result: String`, and the final `Ok(result)` line was changed to `Ok(self.wrap_with_heading_footer(result))` — this is simpler than Tree's 3-branch convergence (Phase 2) or Expanded's 2-return-point wiring (Phase 3), since Text had exactly one output-producing method and one return statement to change. `target_width` computed identically to Tree/Expanded: max display width across the already-rendered body's lines (Text output is equally ragged — line shape varies by `TextVariant`). 6 new tests (`tests/text_heading_test.rs`, FT-31..FT-36, including an FT-36 branch-coverage case proving the wrap still applies when `format_cli_help()`'s own internal `data.rows.is_empty()` early return produces an empty body — mirrors Tree's FT-24 and Expanded's FT-30). Confirmed the `format_text` feature's two-level cfg-gating convention (`#![cfg(feature = "enabled")]` at file scope, `#[cfg(feature = "format_text")] mod {...}` wrapping the tests) by reading two existing Text test files before writing the new one — a structural difference from Tree/Expanded's simpler single-level gating that the new test file correctly follows. Doc sweep completed across `docs/algorithm/007_heading_rendering.md`, `docs/invariant/005_heading.md`, `docs/feature/007_table_heading.md` (added "Text usage" construction example; removed "Text" from the not-yet-implemented list, leaving only Yaml/Toml/Sql), `docs/api/003_config_types.md` (new `#### TextFormatter` subsection — confirmed via reading `docs/formatter/003_tree_formatter.md` that the formatter-specific `docs/formatter/*.md` layer was deliberately left untouched in Phases 1–3, so Phase 4 stayed consistent by extending `003_config_types.md` only, not `004_formatters.md` or `010_text_formatter.md`), all 3 `tests/docs/` counterparts, and `tests/readme.md`. Live verification during implementation: `cargo check -p data_fmt --all-features` clean; `cargo nextest run -p data_fmt --all-features --test text_heading_test` → 6/6 pass; `cargo nextest run -p data_fmt --all-features` (full suite) → 979/979 pass; `cargo clippy -p data_fmt --all-targets --all-features -- -D warnings` → clean.

## Related Documentation

- `src/config/table_heading.rs` — the `Heading` type generalized in Phase 0.
- `docs/feature/007_table_heading.md`, `docs/algorithm/007_heading_rendering.md`, `docs/invariant/005_heading.md`, `docs/api/003_config_types.md` — extended in Phase 1; will be extended again per subsequent phase.
- `task/completed/009_api_consistency_with_prefix.md` — established the `with_` builder-prefix convention this task follows (`with_footer`), and the precedent that broad additive API growth does not by itself require a version bump.
- `task/unverified/015_add_heading_render_method.md` — a separate, not-yet-verified task proposing a standalone `Heading::render(width)` public method; related but independent (that task is about exposing the primitive directly to external callers, this task is about wiring it into more formatters internally).

## Verification Record

**Gate Round 1** (Tier 2 — Dual-Role Self-Check, one-shot, self-administered by ai; no subagent dispatch — default tier, no explicit user request for a heavier tier was made this session)

- **Date:** 2026-08-22
- **Result:** PASS — 8/8 dimensions 🟢 (0 Blocking)

| Gate | Name | Prev | Now | Issues | Fixes |
|------|------|------|-----|--------|-------|
| D1 | Scope Coherence | — | 🟢 | — | — |
| D2 | MOST Goal Quality | — | 🟢 | — | — |
| D3 | Value / YAGNI | — | 🟢 | Confirming pass: reuses existing, proven rendering code — low risk, closes a real gap (position + formatter-coverage restrictions were accidental, not deliberate). Adversarial pass: is a footer genuinely wanted anywhere, or is this speculative? No named external consumer exists (unlike 016/017's glassbox/wplan citations) — this is accepted as an internal API-completeness improvement (symmetric to `with_heading`, near-zero marginal cost since the primitive already exists) rather than a consumer-driven need; flagged as non-blocking since the same judgment call (016 `duration_6ch` precedent) was already accepted once before, but noted here for transparency rather than silently omitted. | — |
| D4 | Implementation Readiness | — | 🟢 | Adversarial check: Phase 1 confirmed the pattern works end-to-end (compiles, 35/35 tests, clean clippy). Phases 2–5 are not yet de-risked — Tree's header in particular depends on a streaming-vs-buffer question not yet answered by reading the source. Marked non-blocking: Work Procedure explicitly defers each phase's implementation detail to "read the source first," not assumed. | — |
| D5 | Execution Scope | — | 🟢 | All touched paths inside `module/core/data_fmt` (`$SCOPE_DIR` = package root); no sibling-crate edits required or made | — |
| D6 | Crate Scope Unity | — | 🟢 | Single crate (`data_fmt`); no workspace-root or cross-crate file touched | — |
| D7 | Crate Locality | — | 🟢 | `data_fmt` already owns `Heading` and all formatter implementations — no new crate boundary crossed | — |
| D8 | Crate Single Responsibility | — | 🟢 | Remains "formatting primitives"; header/footer on more formatters is the same responsibility applied more broadly, not a new concern | — |
| **Total** | | — | 🟢 | 1 non-blocking D3 note (no named external consumer, accepted per 016 precedent) | 8/8 PASS |

**Gate Check** · Tier: 2 · Type: Full · Verdict: PASS · Agents: 0 (self, dual-role) · 1/1

Phase 1 completion itself (separate from the task-readiness gate above): confirming pass — compiled, 35/35 tests green, clippy clean, docs extended across all 7 required files. Adversarial pass — checked for silent scope creep (none: `Heading`'s data shape unchanged, no existing call site's output altered), checked the one real defect found (clippy `unused_self`) was root-caused and fixed properly (associated function, not `#[allow]`) rather than suppressed, checked the one test failure (FT-9) was independently reproduced as pre-existing/unrelated rather than assumed away (isolated single-test re-run before and after the fix, both green).

| Gate | Name | Prev | Now | Issues | Fixes |
|------|------|------|-----|--------|-------|
| G1 | Compiles clean | — | 🟢 | — | — |
| G2 | Full test pass (35/35) | — | 🟢 | FT-18 fixture had mismatched title lengths ("Top"/"Bottom") that tripped the width-ceiling clamp on an assertion assuming both fit — not a source defect | Changed footer title "Bottom"→"End" (3 chars, matches "Top") |
| G3 | Clippy clean | 🔴 | 🟢 | `unused_self` on `render_rule_if_present` — the position-agnostic refactor left `&self` unused in the body | Removed `&self`, made it an associated function; updated both call sites to `Self::` |
| G4 | Docs extended, not replaced | — | 🟢 | — | — |
| G5 | No pre-existing test regressed | — | 🟢 | FT-9 failed once under full concurrency (nested `cargo run` racing the outer `nextest run`'s `target/` usage) | Reproduced in isolation both before and after the clippy fix — passes standalone both times; confirmed passing again in the final full concurrent run |
| **Total** | | 🔴 | 🟢 | 0 open | 2/2 fixed |

**Gate Check** · Tier: 2 · Type: Full · Verdict: PASS · Agents: 0 (self, dual-role) · 1/1

Phase 2 completion itself (separate from the task-readiness and Phase-1-completion gates above): confirming pass — compiled, 6/6 new Tree tests green (FT-19..FT-24), full suite 967/967 green, clippy clean, docs extended across all 6 required files (algorithm, invariant, feature, api, 3× `tests/docs/`, `tests/readme.md`). Adversarial pass — checked for silent scope creep (none: `format_aligned()`/`format_with_aggregation()` deliberately left untouched per the task's own approved Phase 2 scope, only the primary `format()` method wired); checked that relocating `render_rule_if_present`/`Heading::render_line()` out of `TableFormatter`'s impl block into formatter-agnostic `config/table_heading.rs` changed zero existing Table behavior (confirmed via a fresh full-suite regression run after the entire doc sweep, not merely assumed from the diff looking safe); checked the one doc-drift defect found (FT-18 spec text in `tests/docs/feature/007_table_heading.md` still described the old "Bottom" footer title after Phase 1's fixture rename to "End") was caught and fixed on discovery rather than left; checked a stray `../` path segment that appeared in one Edit tool call resolved to the canonical file with no stray or corrupted file left behind (verified via `find` + `grep`, not assumed from the tool call reporting success).

| Gate | Name | Prev | Now | Issues | Fixes |
|------|------|------|-----|--------|-------|
| G1 | Compiles clean | — | 🟢 | — | — |
| G2 | Full test pass (967/967 incl. 6 new Tree tests) | — | 🟢 | — | — |
| G3 | Clippy clean | — | 🟢 | — | — |
| G4 | Docs extended, not replaced | — | 🟢 | Pre-existing (Phase 1) doc drift found while reading for Phase 2: FT-18 spec still said "Bottom" after the source fixture was renamed to "End" | Fixed FT-18 Given/Then text to match "End" |
| G5 | No pre-existing test regressed | — | 🟢 | — | — |
| G6 | Rendering primitive stays formatter-agnostic (no Table-specific state referenced) | — | 🟢 | — | — |
| **Total** | | — | 🟢 | 1 non-blocking doc-drift note (inherited from Phase 1, fixed on discovery) | 1/1 fixed |

**Gate Check** · Tier: 2 · Type: Full · Verdict: PASS · Agents: 0 (self, dual-role) · 1/1

Phase 3 completion itself (separate from the task-readiness, Phase-1-completion, and Phase-2-completion gates above): confirming pass — compiled, 6/6 new Expanded tests green (FT-25..FT-30), full suite 973/973 green, clippy clean (all confirmed via live tool calls earlier in this same uncompacted session — not re-run for the doc-only sweep, since no compaction occurred in between and doc edits cannot change compiled behavior), docs extended across all 7 required files (algorithm, invariant, feature, api, 3× `tests/docs/`, `tests/readme.md`). Adversarial pass — checked for silent scope creep (none: `record_separator` left untouched, heading/footer wired only into `wrap_with_heading_footer()`'s own two call points); checked the field-visibility fork was a deliberate, justified deviation from Tree's pattern rather than an inconsistency — confirmed by reading `expanded_config.rs` in full before editing, found all 7 pre-existing fields already `pub` with zero accessor methods anywhere in the file, so matching that local convention (not Tree's) is the internally-consistent choice; checked the empty-headers branch-coverage test (FT-30) actually exercises the early-return path claimed — traced `format_view()`'s `if headers.is_empty() { return self.wrap_with_heading_footer(String::new()); }` directly, confirmed `RowBuilder::new(vec![]).build_view()` produces a zero-header view that hits exactly this branch; checked the pre-existing `#### WrapConfig` doc/code drift found during the sweep (claimed `ExpandedConfig` fields were private) was independently verified against the actual source before fixing, not assumed from the WrapConfig text's own claim.

| Gate | Name | Prev | Now | Issues | Fixes |
|------|------|------|-----|--------|-------|
| G1 | Compiles clean | — | 🟢 | — | — |
| G2 | Full test pass (973/973 incl. 6 new Expanded tests) | — | 🟢 | — | — |
| G3 | Clippy clean | — | 🟢 | — | — |
| G4 | Docs extended, not replaced | — | 🟢 | Pre-existing doc/code drift found while extending `api/003_config_types.md`: `#### WrapConfig` claimed `ExpandedConfig`'s fields were private — they are actually all `pub` (confirmed by reading `expanded_config.rs`) | Corrected the `#### WrapConfig` claim to accurately describe `ExpandedConfig` as also all-`pub` |
| G5 | No pre-existing test regressed | — | 🟢 | — | — |
| G6 | `record_separator` and heading/footer stay distinct concepts | — | 🟢 | — | — |
| **Total** | | — | 🟢 | 1 non-blocking doc-drift note (pre-existing, fixed on discovery) | 1/1 fixed |

Phase 4 completion itself (separate from the task-readiness and Phase-1/2/3-completion gates above): confirming pass — compiled, 6/6 new Text tests green (FT-31..FT-36), full suite 979/979 green, clippy clean (all confirmed via live tool calls earlier in this same uncompacted session — not re-run for the doc-only sweep, since no compaction occurred in between, no `.rs` file was touched during the sweep, and doc-only edits cannot change compiled behavior), docs extended across all 7 required files (algorithm, invariant, feature, api, 3× `tests/docs/`, `tests/readme.md`). Adversarial pass — checked for silent scope creep (none: the wrap is wired into exactly one point, `Format::format()`'s final `Ok(result)` line, which the summary and a re-read of the file both confirm is the sole output path — no separate `write_to`/streaming method exists on `TextFormatter` to also wire); checked the `docs/api/` placement decision (extend `003_config_types.md` with a new `#### TextFormatter` subsection, rather than `004_formatters.md` or `docs/formatter/010_text_formatter.md`) against actual precedent rather than assuming — read both `docs/formatter/003_tree_formatter.md` and `docs/formatter/002_expanded_formatter.md` and confirmed neither mentions heading/footer at all, proving that doc layer was deliberately left untouched in Phases 1–3, so Phase 4 stayed consistent by not introducing a new doc-touch-point; checked the new `#### TextFormatter` section's claimed constructor and builder method names (`new`, `bullets`, `numbered`, `key_value`, `sections`, `compact`, `cli_help`, `with_indent`, `with_separator`, `with_heading`, `with_footer`) against the actual source via `grep -n "pub fn"` rather than trusting the earlier implementation summary — all matched exactly. **One real defect found and fixed**: the new "Text usage" illustrative example in `docs/feature/007_table_heading.md` showed bullet rows as `"• Alice, 30"` (comma-separated) — grepping `format_bullets()` in `src/formatters/text.rs` (line 283) showed it actually joins row cells with a plain space (`row_text.join( " " )`), producing `"• Alice 30"` with no comma; corrected both example lines.

| Gate | Name | Prev | Now | Issues | Fixes |
|------|------|------|-----|--------|-------|
| G1 | Compiles clean | — | 🟢 | — | — |
| G2 | Full test pass (979/979 incl. 6 new Text tests) | — | 🟢 | — | — |
| G3 | Clippy clean | — | 🟢 | — | — |
| G4 | Docs extended, not replaced | — | 🟡 | "Text usage" example in `docs/feature/007_table_heading.md` showed comma-separated bullet rows (`"• Alice, 30"`); actual `format_bullets()` joins cells with a space, no comma | Corrected both example lines to `"• Alice 30"` / `"• Bob 25"` |
| G5 | No pre-existing test regressed | — | 🟢 | — | — |
| G6 | `docs/api/` placement matches established Phase 1–3 precedent (no new doc-touch-point introduced without verifying prior phases' choice) | — | 🟢 | Verified by reading `docs/formatter/002_expanded_formatter.md` and `003_tree_formatter.md` — neither mentions heading/footer, confirming that layer was deliberately untouched in Phases 1–3 | — |
| **Total** | | — | 🟢 | 1 non-blocking doc-content defect (found and fixed via adversarial grep-verification, not assumed correct) | 1/1 fixed |

**Gate Check** · Tier: 2 · Type: Full · Verdict: PASS · Agents: 0 (self, dual-role) · 1/1
