# Input Type Test Spec Doc Entity

### Scope

- **Purpose**: Provide test coverage specifications for all input type doc instances.
- **Responsibility**: Registry and overview of all input type test spec instances.
- **In Scope**: IV-N input type contract cases in Given/When/Then format for all 2 input type elements (`table_view`, `tree_node`); minimum 4 cases per spec.
- **Out of Scope**: Data model invariants (see `../invariant/`), builder API contracts (see `../builder/`).

### Overview Table

| ID | Name | Purpose | Status |
|----|------|---------|--------|
