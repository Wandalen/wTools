
[package]
name = "typing_tools"
version = "0.2.0"
edition = "2021"
authors = [
  "Kostiantyn Wandalen <wandalen@obox.systems>",
  "Dmytro Kryvoruchko <dmytro.kr@obox.systems>",
]
license = "MIT"
readme = "Readme.md"
documentation = "https://docs.rs/typing_tools"
repository = "https://github.com/Wandalen/wTools/tree/master/module/core/typing_tools"
homepage = "https://github.com/Wandalen/wTools/tree/master/module/core/typing_tools"
description = """
Collection of general purpose tools for type checking.
"""
categories = [ "algorithms", "development-tools" ]
keywords = [ "fundamental", "general-purpose" ]

[package.metadata.docs.rs]
features = [ "full" ]
all-features = false

exclude = [ "/tests", "/examples", "-*" ]

[features]

default = [
  # "implements",
  "enabled",
  "is_slice",
  "inspect_type",
]

full = [
  # "use_alloc",
  # "implements",
  "enabled",
  "is_slice",
  "inspect_type",
]
# use_std = []
no_std = []
use_alloc = []
enabled = []

inspect_type = [ "inspect_type/enabled" ]
is_slice = [ "is_slice/enabled" ]
implements = [ "implements/enabled" ]
nightly = [ "inspect_type/nightly" ]

[dependencies]

## internal
inspect_type = { workspace = true }
is_slice = { workspace = true }
implements = { workspace = true }

[dev-dependencies]
test_tools = { workspace = true }
smoke_test = { workspace = true }
