//! Deprecated: renamed to [`data_fmt`](https://crates.io/crates/data_fmt).
//!
//! This crate is a thin re-export shim for backward compatibility.
//! All types, traits, and formatters are re-exported from `data_fmt`.
//! Use `data_fmt` for all new projects — the API is identical.

pub use data_fmt::*;
