[package]
name = "clone_dyn_types"
version = "0.42.0"
edition = "2021"
authors = [
  "Kostiantyn Wandalen <wandalen@obox.systems>",
]
license = "MIT"
readme = "readme.md"
documentation = "https://docs.rs/clone_dyn_types"
repository = "https://github.com/Wandalen/wTools/tree/master/module/core/clone_dyn_types"
homepage = "https://github.com/Wandalen/wTools/tree/master/module/core/clone_dyn_types"
description = """
Derive to clone dyn structures.
"""
categories = [ "algorithms", "development-tools" ]
keywords = [ "fundamental", "general-purpose" ]

[lints]
workspace = true

[package.metadata.docs.rs]
features = [ "full" ]
all-features = false

[features]

default = [ "enabled" ]
full = [ "enabled" ]
enabled = []

[dependencies]

[dev-dependencies]
# test_tools = { workspace = true, features = [ "full" ] }  # REMOVED: Creates circular dependency via macro_tools → clone_dyn_types → test_tools → impls_index_meta → macro_tools
# inspect_type = { workspace = true, features = [ "full" ] }
