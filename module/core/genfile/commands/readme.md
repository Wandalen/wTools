# commands/

YAML command definitions loaded at runtime by the unilang command registry.

### Responsibility Table

| File | Responsibility |
|------|----------------|
| archive.yaml | Archive lifecycle command definitions (FR1) |
| file.yaml | File management command definitions (FR2) |
| parameter.yaml | Parameter management command definitions (FR3) |
| value.yaml | Template value command definitions (FR4) |
| content.yaml | Content transformation command definitions (FR5) |
| materialize.yaml | Template materialization command definitions (FR6) |
| pack.yaml | Archive pack and unpack command definitions (FR7) |
| analysis.yaml | Analysis and inspection command definitions (FR8) |
