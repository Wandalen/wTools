[package]
name = "variadic_from_meta"
version = "0.30.0"
edition = "2021"
authors = [
  "Kostiantyn Mysnyk <wandalen@obox.systems>",
]
license = "MIT"
readme = "readme.md"
documentation = "https://docs.rs/variadic_from_meta"
repository = "https://github.com/Wandalen/wTools/tree/master/module/core/variadic_from_meta"
homepage = "https://github.com/Wandalen/wTools/tree/master/module/core/variadic_from_meta"
description = """
Variadic from, proc-macro part.
"""
categories = [ "algorithms", "development-tools" ]
keywords = [ "fundamental", "general-purpose" ]

[lints]
workspace = true

[lib]
proc-macro = true

[dependencies]
macro_tools = { workspace = true, features = ["enabled", "struct_like", "generic_params", "typ", "diag"] }

[dev-dependencies]
test_tools = { workspace = true, features = [ "full" ] }
variadic_from = { workspace = true, features = [ "full" ] }

[features]
default = []
full = [ "enabled" ]
enabled = []
