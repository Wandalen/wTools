# Postponed Crates

Crates that have been temporarily disabled due to compilation or test failures.

All crates in this directory are automatically excluded from the workspace via:
```toml
exclude = [ "module/postponed/*" ]
```
