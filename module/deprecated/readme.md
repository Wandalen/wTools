# deprecated/

Crates parked here after being marked `dep` in `doc/layers.md`. Excluded from the workspace (`Cargo.toml` exclude list). Not compiled, not tested, not published. Retained only to preserve git history and docs before final deletion.

## Responsibility Table

| Path | Responsibility |
|------|----------------|
| `willbe2/` | Deprecated transparent re-export of `willbe` |
