# bin/

Binary entry points. Each file delegates to `run_cli()` in `src/cli.rs`.

### Responsibility Table

| File | Responsibility |
|------|----------------|
| `pt.rs` | Short-alias entry point for the program_tools CLI |
