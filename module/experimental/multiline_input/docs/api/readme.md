# API Docs

### Scope

API documentation for `multiline_input`. Each file documents one API surface with its public interface and contract.

### Responsibility Table

| File | Responsibility |
|------|----------------|
| [001_multiline_input.md](001_multiline_input.md) | Public `collect()` function, `Builder` type, and `TerminalOps` trait |
