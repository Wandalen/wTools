[package]
name = "winterval"
version = "0.3.0"
edition = "2021"
authors = [
  "Kostiantyn Mysnyk <wandalen@obox.systems>",
  "Dmytro Kryvoruchko <dmytro.kr@obox.systems>",
]
license = "MIT"
readme = "readme.md"
documentation = "https://docs.rs/winterval"
repository = "https://github.com/Wandalen/wTools/tree/master/module/experimental/winterval"
homepage = "https://github.com/Wandalen/wTools/tree/master/module/experimental/winterval"
description = """
Interval adapter for both open/closed implementations of intervals ( ranges ).
"""
categories = [ "algorithms", "development-tools" ]
keywords = [ "fundamental", "general-purpose" ]

[lints]
workspace = true

[package.metadata.docs.rs]
features = [ "full" ]
all-features = false



[features]
default = [ "enabled" ]
full = [ "enabled" ]
enabled = [ "dep:interval_adapter", "interval_adapter/enabled" ]
no_std = []
use_alloc = [ "no_std" ]

[dependencies]
interval_adapter = { workspace = true, optional = true }

[dev-dependencies]
test_tools = { workspace = true, features = [ "full", "standalone_build" ] }
