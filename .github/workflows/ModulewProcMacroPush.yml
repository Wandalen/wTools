name : wproc_macro

on : push

env :
  CARGO_TERM_COLOR : always

jobs :

  # wproc_macro

  test :
    uses : Wandalen/wTools/.github/workflows/StandardRustPush.yml@master
    with :
      manifest_path : 'module/rust/wproc_macro/Cargo.toml'
      module_name : 'wproc_macro'
