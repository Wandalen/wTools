# verb

Workspace-root verb directory per `verb.rulebook.md` (`$GENAI/verb/verb.rulebook.md`) — named executable scripts implementing the `do` protocol's canonical verbs. Package-level `verb/` (none yet in this workspace) would take precedence over this one for package-scope invocations.

## Responsibility Table

| File | Responsibility |
|------|-----------------|
| `test` | Full-suite Final Verification (VE009), gated on interactive `/dev/tty` confirmation — see `../rulebook.md` |
