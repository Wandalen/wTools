# Scan and documentation policy for the wTools workspace.
# Written by human. Read by .locale.scan and .locale.doc.generate.

scan:
  depth: 3           # crates at module/core/<name>/ and module/experimental/<name>/ and module/alias/<name>/
  exclude:
    - "*/.git"
    - "*/target"

doc:
  depth: 1
